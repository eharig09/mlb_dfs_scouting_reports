beautifulsoup4
fpdf2
lxml
matplotlib
MLB-StatsAPI
numpy
openpyxl
pandas
# pandas 3.x stores strings as PyArrow-backed by default, so the cached report payloads
# in .cache/report_data cannot be unpickled without it.
pyarrow
pybaseball
requests
scikit-learn
scipy
seaborn
selenium
tqdm
webdriver-manager

# Tests. Not needed to run the tools; needed to change them with confidence.
pytest

# Streamlit dashboard (added 2026-08-20)
streamlit>=1.62
altair>=6.2
